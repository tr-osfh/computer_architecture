name: "assert is_binary_palindrome(5) == 0"
limit: 2000
memory_size: 0x1000
memory_mapped_io:
  0x80: [4026531855]
  0x84: []
reports:
  - name: step-by-step
    slice: all
    view: |
      
  - name: Check results
    slice: last
    filter:
      - state
    view: |
      numio[0x80]: {io:0x80:dec}
      numio[0x84]: {io:0x84:dec}
    assert: |
      numio[0x80]: [] >>> []
      numio[0x84]: [] >>> [1]